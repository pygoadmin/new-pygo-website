import React from "react";
import Layout from "../components/Layout";
import styled from "styled-components";

const about = () => {
  return (
    <AboutPage>
      <Layout>Hello from about</Layout>
    </AboutPage>
  );
};

const AboutPage = styled.section``;

export default about;
